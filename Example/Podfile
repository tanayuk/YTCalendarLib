use_frameworks!

target 'YTCalendarLib_Example' do
  pod 'YTCalendarLib', :path => '../'

  target 'YTCalendarLib_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.0.0'
  pod 'Nimble', '~> 5.1.1'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
  end
end
